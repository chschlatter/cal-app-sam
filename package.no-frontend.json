{
  "name": "cal-app-sam",
  "description": "Calendar App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "cookie": "^0.6.0",
    "dayjs": "^1.11.10",
    "esbuild": "^0.19.8",
    "google-auth-library": "^9.6.3",
    "http-errors": "^2.0.0",
    "jsonwebtoken": "^9.0.2",
    "node-polyglot": "^2.5.0",
    "uuid": "^9.0.1"
  },
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy",
    "build-dev": "sam build --config-env dev",
    "deploy-dev": "sam deploy --config-env dev"
  },
  "devDependencies": {
    "@aws-sdk/lib-dynamodb": "^3.511.0",
    "@types/aws-lambda": "^8.10.133"
  }
}
